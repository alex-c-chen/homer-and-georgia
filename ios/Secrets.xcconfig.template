// Copy this file to Secrets.xcconfig (gitignored) and fill in the real values.
// Secrets.xcconfig is read by the Xcode build system and baked into the app bundle
// via Info.plist, so secrets never live in source code.

API_SECRET = your-api-secret-here
